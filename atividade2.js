let par = 0 
let impar = 0
let numeros = []

for (let i = 0 ; i < 10; i ++);

     numeros = parseInt (prompt(`Digite um valor`));



    if (numeros % 2 == 0){
       let = par++

        document.write(`O número ${par }é par`);
    }
    else{
       let = impar++

        document.write(`O número ${impar} é ímpar`);
    }

